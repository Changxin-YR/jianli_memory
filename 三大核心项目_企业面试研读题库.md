# 三大核心项目｜企业面试研读题库

> 目标：先把三个核心项目真正读熟，再进入高压模拟面试。
>
> 项目事实来源：
> - Changxin-YR/yuxin
> - Changxin-YR/yunlin
> - Changxin-YR/agent
>
> 规则：以下答案只使用仓库当前能够支持的项目事实。不要把不同项目技术栈串用。

---

# 一、渔芯 AI 水产养殖一体化系统

## 1. 先记住整体定位

渔芯不是一个“鱼塘 CRUD Demo”，而是一套面向水产养殖企业的生产经营一体化系统。

主要业务链：

```text
基地 / 区域 / 塘口
        ↓
养殖批次
        ↓
投苗 / 投喂 / 出塘
        ↓
仓储库存
        ↓
采购 / 销售
        ↓
成本归集 / 收付款
```

权限控制链：

```text
登录用户
  ↓
角色 / Permission
  ↓
DataScope
  ↓
Capability
  ↓
Domain Service
  ↓
UnitOfWork
  ↓
MySQL
```

人工页面调用链：

```text
Vue3
→ HTTP
→ Flask
→ Capability Route
→ CapabilityRunner
→ Permission / DataScope / Schema / Confirmation / Idempotency / Invariants
→ Domain Service
→ UnitOfWork
→ PyMySQL
→ MySQL
```

Agent 调用链：

```text
用户自然语言
→ DeepSeek Harness
→ Agent Tool
→ Agent Gateway
→ CapabilityRunner
→ Domain Service
→ UnitOfWork
→ MySQL
```

### 投喂真实业务链

```text
feeding.create
→ 创建 draft 投喂单
→ 不立刻扣物料库存

feeding.verify
→ 权限 / DataScope / 状态 / 版本检查
→ warehouse 受控入口扣物料库存
→ inventory_ledger
→ 成本归集
→ feeding 变为 verified
```

注意：投喂消耗的是物料库存，不减少塘内鱼的存塘；出塘核验才会减少存塘。

---

## 2. 面试问题与标准答案

### Q1：请介绍一下渔芯项目。

**标准答案：**

渔芯是一套面向水产养殖企业的生产经营管理系统，业务上把基地、区域、塘口、养殖批次、投喂、仓储、采购、销售和成本这些流程串起来。前端是 Vue3 + TypeScript，后端是 Python + Flask，数据库使用 MySQL，通过 PyMySQL 手写 SQL。

我在项目里重点关注的是业务流程、后端核心能力以及 Agent 接入。比较有特点的设计是 Capability Registry：系统把创建塘口、登记投喂、核验投喂这类业务操作统一声明成 Capability，再围绕这份声明处理 REST 路由、权限、DataScope、确认、幂等、审计、业务不变量和 Agent Tool。真正的业务执行仍然进入 Domain Service，写操作通过 UnitOfWork 控制事务。

---

### Q2：这个项目到底解决什么业务问题？

**标准答案：**

它不是只记录鱼塘，而是解决养殖企业生产、库存、采购、销售和成本之间数据割裂的问题。例如一次投喂不是单独记一条记录，它还可能涉及饲料库存扣减、库存流水和养殖成本归集；一次出塘又会影响存塘、销售等后续业务。因此项目重点是把这些业务事实放进同一套流程和事务约束里。

---

### Q3：Capability Registry 是什么？

**标准答案：**

Capability 是一个业务操作的统一声明，例如 `pond.create`、`feeding.verify`。它会描述这个操作的 HTTP method/path、handler、required_permission、DataScope、risk、confirmation、idempotency、audit、invariants 和 AgentExposure。

Capability 自己不承载全部业务逻辑，真正的业务执行还是在 Domain Service。它主要解决多个入口维护同一业务能力时容易发生规则漂移的问题。

---

### Q4：为什么不直接像普通 Flask 项目一样手写 Route？

**标准答案：**

如果只有简单人工页面，直接写 Route 完全可以。但这个项目同时有 Web 页面和 Agent 两个入口，而且权限、DataScope、确认、幂等和审计都需要保持一致。

如果 Route、Agent Tool 和权限配置各自维护一份，同一个能力修改时很容易漏改。因此我把这些“能力契约”集中到 Capability，Route 和 Agent Tool 围绕 Capability 派生，两个入口最终进入同一套业务服务。

---

### Q5：Capability 会不会变成 God Object？

**标准答案：**

不会把整个系统塞进一个 Capability。设计上是一条业务操作对应一个 Capability，例如 `feeding.create` 和 `feeding.verify` 是两条独立能力。Capability 主要是声明和策略；真正复杂的业务流程还是在各自 Domain Service 中。

当然它会增加一层抽象，所以小型 CRUD 项目未必值得这样做。这个项目有多个业务域和 Web/Agent 多入口，我认为换取规则一致性是值得的。

---

### Q6：Capability 和 Service 有什么区别？

**标准答案：**

Capability 更像“业务能力契约”，回答这个操作叫什么、怎么暴露、需要什么权限、数据范围、风险和业务约束。

Service 回答的是“这个业务到底怎么执行”，比如核验投喂时如何检查状态、扣库存、写流水和归集成本。

一句话：Capability 管声明和统一约束，Service 管业务执行。

---

### Q7：为什么 Service 里还要重新做权限检查？Runner 不是已经检查了吗？

**标准答案：**

这是纵深防御。上层会先做权限检查，但 Service 不能假定所有调用者永远都正确经过了某一个入口。未来增加新入口，或者某层出现 Bug，都可能绕开前面的校验。

所以重复的是“检查动作”，不是重新实现一套权限规则。最终业务执行层仍然要有最后一道保护。

---

### Q8：RBAC 和 DataScope 有什么区别？

**标准答案：**

RBAC 解决“能不能做”，DataScope 解决“能对哪些数据做”。

例如两个用户最终权限集合里都有 `pond.view`，不代表他们角色一定相同，也不代表看到的数据相同。用户 A 可能只拥有某个区域的数据范围，用户 B 可能只拥有另一个塘口的数据范围。

---

### Q9：DataScope 为什么不能只在前端做？

**标准答案：**

前端只是用户可控环境，不能作为安全边界。用户可以通过 DevTools、Postman、curl、脚本甚至 Agent 直接构造后端请求。

所以真实数据范围必须由服务端根据当前登录用户从数据库解析，再落到查询或单行访问中。前端可以做显示优化，但不能负责真正的数据安全。

---

### Q10：DataScope 最后是怎么落到 SQL 的？

**标准答案：**

用户的数据范围先由 DataScopeResolver 从数据库解析成统一的 Scope 对象。列表查询时 Service 使用 Scope 的公共方法生成 SQL 谓词，例如某个区域最终会形成类似 `p.area_id IN (%s)` 的参数化条件。

单条详情或修改时，会先读取目标行，再用 `scope.allows_row(row)` 判断这一行是否属于当前用户。

所以 Service 可以有不同 SQL，但不能各自重新解释 DataScope。

---

### Q11：DataScope 配错了怎么办？

**标准答案：**

项目采用 fail-closed。普通账号没有有效数据范围，或者范围无法解析时，不会默认放开全部数据，而是拒绝业务访问。

这样安全失败方向是“看不到”，不是“误看到全部”。

---

### Q12：feeding.create 和 feeding.verify 为什么要拆开？

**标准答案：**

`feeding.create` 先创建 draft 投喂记录，真正影响库存和成本的是 `feeding.verify`。

这样可以把“登记业务单据”和“产生正式副作用”分开。核验阶段统一检查权限、DataScope、状态、乐观锁、库存、会计期间等规则，并把库存扣减和成本归集放在一致的事务边界中。

---

### Q13：投喂核验为什么会影响仓储和成本？

**标准答案：**

投喂会实际消耗饲料，所以核验后要从仓储域扣减物料库存并写库存流水。同时饲料本身有采购成本，因此实际消耗的价值还要归集到对应养殖批次。

生产域不直接改仓储或成本表，而是调用各自领域提供的受控入口，让库存规则和成本规则仍然由对应领域负责。

---

### Q14：为什么 production 不直接 UPDATE warehouse 表？

**标准答案：**

因为库存属于 warehouse 域。如果多个业务域都直接改库存表，库存规则就会散落到很多地方，负库存、批次选择、流水规则等很容易漂移。

生产域只表达“发生一次饲料消耗”，真正库存如何变化由 warehouse 域负责，这样规则所有权更清楚。

---

### Q15：UnitOfWork 是什么？

**标准答案：**

UnitOfWork 用来定义一次业务操作的事务边界。一个业务操作内需要保持一致的多条数据库写入使用同一个 UOW，正常退出统一 commit，中间任何一步异常则整体 rollback。

它的核心不是封装连接，而是防止每个 Repository/Service 自己随意 commit，导致跨步骤业务出现部分成功。

---

### Q16：为什么 Repository 自己 commit 很危险？

**标准答案：**

例如投喂核验中先扣库存，Repository 自己 commit；之后成本归集失败，那么外层已经无法把库存一起回滚，系统就会出现库存变了但成本没变。

所以子步骤不能独立决定提交，事务应该由外层业务操作统一控制。

---

### Q17：项目里的写操作什么时候才算成功？

**标准答案：**

以当前运行代码为准：业务写、业务不变量检查、reload after 和 success audit 都在事务边界内完成；只有 UnitOfWork 正常 commit 后，Runner 才向调用方返回 `executed`。

模型自己没有权力宣布成功，它只能转述服务端结构化结果。

---

### Q18：为什么要有幂等？

**标准答案：**

幂等主要解决重复请求带来的重复副作用。例如用户重复点击、网络重试或者 Agent 重放，同一个逻辑请求不能重复创建记录、重复扣库存或重复付款。

项目通过 Idempotency-Key 和服务端幂等记录识别重复调用，并处理 replay、processing、failed、COMMIT_UNKNOWN 等状态。

---

### Q19：乐观锁解决什么问题？

**标准答案：**

乐观锁主要防止“读到旧版本后覆盖别人刚刚完成的修改”。通常通过 row_version / expected_version 判断。

如果 UPDATE 时版本已经变化，受影响行数为 0，就返回版本冲突，要求刷新后重试。

---

### Q20：悲观锁/行锁解决什么问题？

**标准答案：**

行锁更适合库存等必须串行保护关键资源的场景。读取并修改关键库存记录时使用 `SELECT ... FOR UPDATE` 一类机制，可以让并发事务围绕同一行排队，减少超扣或竞争。

乐观锁和行锁解决的不是同一个问题，要结合业务选择。

---

### Q21：为什么不用 SQLAlchemy？

**标准答案：**

这是项目的明确技术取舍。渔芯使用 PyMySQL + 手写 SQL，希望对 SQL、事务和部分并发行为保持显式控制。

代价是 CRUD、映射和维护代码更多，所以不能说手写 SQL一定优于 ORM。普通业务后台如果更重视开发效率，我也会考虑 SQLAlchemy。

---

### Q22：手写 SQL 怎么防 SQL 注入？

**标准答案：**

用户输入的值通过 PyMySQL 的参数绑定传递，例如 `cursor.execute(sql, params)`，不使用 f-string 把用户输入直接拼进 SQL。

如果是不能参数化的动态列名、排序字段等 SQL 结构，则只能来自服务端白名单。

---

### Q23：Agent 为什么不能直接操作数据库？

**标准答案：**

LLM 不是可信执行主体，可能出现幻觉、提示注入或错误参数。如果直接允许模型访问数据库，很容易绕过权限、DataScope、状态机、事务、幂等和审计。

所以 Agent 只能调用系统明确暴露的业务 Capability，并由服务端重新完成安全和业务校验。

---

### Q24：Agent 的权限是怎么保证的？

**标准答案：**

第一层 Tool Filtering，只把用户有权调用且允许 Agent 使用的工具暴露给模型；第二层 Gateway 在实际调用时再次校验；真正进入业务 Service 时还有最终校验。

多层检查属于纵深防御，权限依据始终来自服务端，而不是模型自己声称的角色。

---

### Q25：这个项目你现在回头看会改什么？

**标准答案：**

可以诚实回答当前仓库已知未完成点，例如部分详情页、生产化进程/反代配置等。技术上我会优先继续收敛文档和运行时代码之间的漂移，并继续减少特殊路径，让更多业务都遵循同一套 Capability/Scope/UOW 机制。

不要编不存在的生产性能问题。

---

### Q26：项目大量用了 AI Coding，哪些是你真正做的？

**标准答案：**

项目确实重度使用 AI Coding 工具提升开发速度，但我不是只把需求丢给模型。我的工作重点是需求拆解、架构约束、关键业务规则、Agent 安全边界、代码审查、联调、Bug 定位和测试验收。

我简历里写出来的核心模块必须做到能读、能解释、能改、能定位问题。AI 生成实现不等于最终正确，需要通过真实数据库 E2E、权限测试和代码检查验证。

---

# 二、云邻 AI 智脑｜物业管理系统

## 1. 项目整体定位

云邻 AI 智脑是一套住宅物业管理平台。

业务覆盖：

```text
社区
→ 楼栋
→ 单元
→ 房屋
→ 住户 / 租客 / 租赁
→ 工单 / 投诉 / 访客
→ 车辆 / 车位
→ 设备 / 巡检
→ 账单 / 收款
```

技术栈：

```text
Flask
+ SQLAlchemy 2.0
+ MySQL
+ Jinja2
+ 原生 CSS/JS
```

权限：

```text
Flask Session
→ user_id + auth_version
→ RBAC
→ DataScope
→ Policy
→ Services / Queries
```

Agent：

```text
浏览器 /ai
→ SSE
→ DeepSeek Harness
→ MCP stdio
→ 动态 MCP Tool
→ Policy / Risk
→ Services
→ SQLAlchemy / MySQL
```

典型工单闭环：

```text
业主报修
→ 经理派单
→ 工程人员接单
→ 维修中
→ 完工
→ 待验收
→ 经理验收
→ 已关闭
→ 业主评价
→ 审计
```

---

## 2. 面试问题与标准答案

### Q1：介绍一下云邻 AI 智脑。

**标准答案：**

云邻 AI 智脑是一套住宅物业管理平台，覆盖房屋、人员关系、租赁、报修工单、投诉、访客、车辆车位、设备巡检和收费等业务。

后端使用 Flask + SQLAlchemy + MySQL，前端是 Jinja2 服务端渲染。写操作统一进入 services.py / services_ops.py，事务、乐观锁、幂等、回读和审计集中处理。权限使用 RBAC + 行级 DataScope，同时内置 DeepSeek Harness Agent，通过 MCP 工具调用同一套业务服务。

---

### Q2：这个项目和渔芯最大的区别是什么？

**标准答案：**

业务领域完全不同；技术实现也不同。

渔芯是 Vue3 + Flask + PyMySQL 手写 SQL，并以 Capability Registry 为核心。

云邻是 Flask + Jinja2 + SQLAlchemy ORM，权限核心是 Policy，Agent 通过 MCP stdio 工具服务接入。

面试时不能把 SQLAlchemy、MCP、Jinja2 说成渔芯的技术。

---

### Q3：为什么物业系统适合做工单状态机？

**标准答案：**

因为工单天然有明确生命周期，例如待派单、已派单、维修中、待验收、已关闭、已取消。不同状态允许的动作不同，例如未派单不能直接验收、已关闭不能重新报完工。

所以状态机可以把合法转移集中约束，避免路由或页面各自判断。

---

### Q4：完整报修流程怎么走？

**标准答案：**

业主先创建报修工单；物业经理根据权限和数据范围查看并派给维修人员；维修人员接单、登记维修过程并报完工；工单进入待验收后由经理验收，最终关闭；业主可以评价。

每个状态变化由服务层统一执行，同时记录流转日志和审计信息。

---

### Q5：为什么所有写操作都要走统一 Service 层？

**标准答案：**

因为写操作往往同时涉及状态、权限、版本、事务、幂等和审计。如果页面路由、Agent Tool 各自直接写 ORM，会形成两套业务逻辑。

所以页面和 AI 都调用统一写命令，业务规则只保留一份。

---

### Q6：SQLAlchemy 在项目里做什么？

**标准答案：**

SQLAlchemy 2.0 用于 ORM 建模、查询和事务管理。项目的 26 张表通过模型表达关系和业务状态，同时使用 SQLAlchemy 的版本字段能力支持乐观锁。

它和渔芯的 PyMySQL 手写 SQL是两个不同取舍。

---

### Q7：SQLAlchemy 的乐观锁怎么做？

**标准答案：**

项目利用 `version_id_col` 管理版本字段。读取对象时拿到当前版本，更新时如果数据库中的版本已经发生变化，SQLAlchemy 能检测到并发冲突，而不是静默覆盖别人的修改。

适合物业工单、账单等多人可能同时操作的业务记录。

---

### Q8：幂等在物业系统里解决什么问题？

**标准答案：**

例如创建工单、建账单、收款这类写操作，如果浏览器重复提交或 Agent 重试，不能重复生成业务记录或重复收款。

所以服务层通过幂等键保证同一逻辑请求只执行一次，并让页面和 Agent 共用同一规则。

---

### Q9：RBAC + DataScope 在物业系统里怎么体现？

**标准答案：**

RBAC 决定角色能做什么，DataScope 决定能看到哪些具体数据。

例如经理和客服可能都有查看工单权限，但只能看到所属小区；维修人员只看到派给自己的工单；业主只能看到自己和自己房屋相关的数据。

项目当前范围包括 all / community / building / assigned / self。

---

### Q10：为什么同一句话，不同账号问 Agent 会得到不同结果？

**标准答案：**

因为 Agent 不拥有独立于 Web 系统之外的权限。MCP Tool 清单按照当前登录用户的权限动态生成，而且实际执行时 Policy 和 DataScope 仍会重新校验。

所以管理员、经理、维修人员和业主能看到的工具和数据都可能不同。

---

### Q11：MCP 在这个项目里是什么角色？

**标准答案：**

MCP stdio 服务是 Agent 和物业业务系统之间的工具协议层。Agent 不是直接 import ORM，也不是直接访问数据库，而是通过 MCP 暴露的工具调用受控业务服务。

工具目录按当前用户权限动态生成，真正执行仍然进入 Policy 和 Service。

---

### Q12：为什么用 MCP，而渔芯用 Agent Gateway？

**标准答案：**

两个项目集成形态不同。云邻通过 DeepSeek Harness + MCP stdio 对接工具生态；渔芯自己定义了基于 Capability 的 Agent Gateway。

共同原则没有变：模型只能访问明确开放的业务能力，不能绕过权限和业务服务直接碰数据库。

---

### Q13：R0–R3 风险分级是什么？

**标准答案：**

项目给 Agent 能力做风险分级。低风险查询或部分允许自动执行的操作可以直接执行；更高风险操作会创建 AiAction，要求用户确认后再真正执行。

尤其 R3 高风险动作一律需要人工确认，不能只依赖模型判断。

---

### Q14：人工确认为什么不是前端弹个 confirm 就完了？

**标准答案：**

因为前端弹窗只是展示。真正需要保存的是服务端认可的待执行动作，包括 payload、payload_hash、目标、风险级别和过期时间。

用户点击确认时，服务端还会重新检查权限、DataScope、目标版本和当前业务状态，防止“确认时看到的是 A，真正执行时已经变成 B”。

---

### Q15：auth_version 是干什么的？

**标准答案：**

短期 Agent 授权令牌绑定 `user_id + auth_version`。如果用户角色、数据范围等权限状态发生变化，auth_version 也会变化，从而让旧令牌和缓存的 Agent Runtime 失效。

这样不会出现管理员已经撤权，但 Agent 还拿着旧权限继续运行。

---

### Q16：为什么 Agent Tool 要动态过滤？

**标准答案：**

没权限的能力最好一开始就不要进入模型上下文，这可以减少攻击面和模型误调用。

但 Tool Filtering 不是唯一安全边界，即使模型硬构造未授权调用，服务端 Policy 仍然会拒绝。

---

### Q17：Jinja2 和 Vue 有什么区别？为什么这里不用 Vue？

**标准答案：**

这个项目用 Jinja2 做服务端渲染，页面结构和业务表单相对传统，部署也更简单，不需要独立前端构建链。

Vue 更适合更复杂的前端状态和组件化交互。云邻的选择不代表 Vue 不适合，而是项目在交付复杂度和交互需求之间做了不同取舍。

---

### Q18：CSRF 为什么需要？

**标准答案：**

项目使用基于 Cookie/Session 的登录方式，浏览器会自动携带 Cookie，所以攻击者可能诱导已登录用户向目标站点发送写请求。

因此写表单需要 CSRF Token，验证请求确实来自可信页面上下文。

---

### Q19：Flask Session 里为什么不能直接存完整权限？

**标准答案：**

项目会在服务端基于用户和 auth_version 重新确认权限状态。权限可能被管理员修改，如果完全信任长时间缓存的客户端会话数据，就容易出现撤权不及时。

Session 更适合表达“当前是谁和会话版本”，真实权限仍然由服务端规则决定。

---

### Q20：为什么写操作还要回读？

**标准答案：**

调用 ORM 方法成功不等于最终业务状态一定符合预期。项目写命令会返回版本和回读结果，用真实数据确认状态，并给审计和 Agent 返回可靠事实。

Agent 只有拿到明确的成功结果后才能告诉用户已经完成。

---

### Q21：SSE 在这个项目里做什么？

**标准答案：**

浏览器的 AI 对话需要连续显示状态、工具调用、模型增量文本、确认动作等事件。SSE 适合服务器向浏览器单向持续推送事件，而且实现比 WebSocket 更轻量。

因此 `/ai/chat` 会把 status / tool / delta / action / done / error 等事件流式回传。

---

### Q22：为什么不用 WebSocket？

**标准答案：**

当前主要需求是服务端持续向浏览器推送 Agent 运行过程，客户端只需发起请求和后续确认，并不需要持续的全双工长连接。

SSE 足够满足需求，而且基于 HTTP，部署和调试更简单。如果未来需要高频双向实时交互，再考虑 WebSocket。

---

### Q23：测试怎么做？

**标准答案：**

项目有 unittest 快速回归，测试环境可使用 SQLite；但最终验收要求在真实 MySQL 上也跑一遍。

此外还有 `seed_demo.py check` 验证演示数据和多角色 DataScope，以及 acceptance_e2e 覆盖真实页面、登录、CSRF、完整工单闭环和 AI SSE。

---

### Q24：SQLite 测试全绿为什么还不够？

**标准答案：**

SQLite 和 MySQL 在类型、约束、锁、SQL 行为等方面并不完全一致。SQLite 很适合快速测试，但不能证明 MySQL 的生产行为一定正确。

所以项目把 SQLite 当快速回归，MySQL 当最终验收的一部分。

---

### Q25：项目最能体现企业业务的一点是什么？

**标准答案：**

不是“接了 Agent”，而是 Agent 和人工页面共用同一套业务服务、权限、DataScope、状态机和审计。

这保证 AI 是系统中的一个受控入口，而不是绕过业务系统的第二套后台。

---

### Q26：如果让你重构这个项目，你优先改什么？

**标准答案：**

可以从当前形态诚实回答：继续减少 services.py / services_ops.py 的集中度，把不同业务域拆得更明确，同时保持统一 Policy/事务契约；另外把更多 Agent Tool 的契约自动化生成，减少能力目录和服务签名之间的维护成本。

这是架构取舍，不要虚构线上性能瓶颈。

---

# 三、Enterprise Smart Assistant（agent 仓库）

## 1. 整体定位

这是一个企业内部办公 Agent。

三类核心能力：

```text
query_policy
→ RAG 查企业制度

check_calendar
→ 查日程

send_email
→ 发送邮件 / 默认 Mock Outbox
```

主调用链：

```text
用户
→ CLI / FastAPI Web
→ LangGraph create_agent
→ DeepSeek
→ Tool Calling
→ Tools Node
→ query_policy / check_calendar / send_email
→ ToolMessage
→ DeepSeek
→ 最终回答
```

RAG：

```text
PDF / MD / TXT
→ 文档加载
→ chunk 500 / overlap 50
→ 本地 BGE embedding
→ Chroma
→ top-k=4
→ ToolMessage 带 source/page
→ LLM 基于证据回答
```

Memory：

```text
thread_id
→ InMemorySaver Checkpointer 保存线程
→ WindowMemoryMiddleware
→ 只把最近 5 轮送给模型
```

Web 流式输出：

```text
POST /api/chat
→ Agent stream
→ token / tool_call / tool_result / approval_required / done / error
→ FastAPI StreamingResponse
→ SSE
→ 浏览器
```

---

## 2. 面试问题与标准答案

### Q1：介绍一下 Enterprise Smart Assistant。

**标准答案：**

这是一个面向企业内部办公场景的 AI Agent。员工可以直接用自然语言查询企业制度、查看日程和发送邮件。

项目使用 LangChain/LangGraph 组织 Agent，DeepSeek 负责理解用户意图和 Tool Calling；制度查询通过 Chroma RAG 完成，并返回来源和页码；短期记忆使用 LangGraph Checkpointer；Web 层使用 FastAPI + SSE 展示模型 token、工具调用和结果。

---

### Q2：为什么这是 Agent，不只是普通聊天机器人？

**标准答案：**

普通聊天机器人主要生成文本，而这个项目会让模型根据用户目标自主选择工具，并在工具执行后根据真实 ToolMessage 继续推理和回答。

例如用户问年假政策时模型选择 `query_policy`，问日程时选择 `check_calendar`，需要发邮件时选择 `send_email`。

---

### Q3：工具是怎么选的？

**标准答案：**

由模型的 Tool Calling 选择，不是代码写关键词 if/else。

工具通过 LangChain `@tool` 注册，模型拿到工具 schema 后产生结构化 tool_calls，LangGraph Tools Node 真正执行，然后把 ToolMessage 回灌模型。

项目还用静态测试守护，避免退化成关键词路由。

---

### Q4：LangChain 和 LangGraph 分别做什么？

**标准答案：**

LangChain 主要负责模型封装、Prompt、`@tool`、LCEL 等组件。

LangGraph 在这个项目里承担 Agent 运行时，包括模型节点和工具节点循环、Checkpointer、thread_id 会话隔离和人工中断/恢复等状态化能力。

---

### Q5：为什么 DeepSeek 可以用 ChatOpenAI 接？

**标准答案：**

项目使用 DeepSeek 的 OpenAI 兼容接口，通过配置 `base_url=https://api.deepseek.com` 和对应模型即可复用 ChatOpenAI 客户端。

ChatOpenAI 在这里是 SDK 接口适配，不代表实际调用的是 OpenAI 模型。

---

### Q6：RAG 的完整链路是什么？

**标准答案：**

先加载 PDF/MD/TXT，按默认 500 字符、50 overlap 切分，使用本地 BGE embedding 生成向量，存入 Chroma。

查询时 `query_policy` 把用户问题交给 retriever，取 top-k=4 chunk，并把 chunk 的 source 和 page metadata 一起返回给模型。模型只能基于这些证据组织答案。

---

### Q7：为什么使用 RAG，不直接把 PDF 全塞到 Prompt？

**标准答案：**

完整文档可能很长，会占用大量上下文，而且每次请求都重复传输。RAG 只检索与当前问题相关的片段，可以控制上下文长度和成本，也更容易提供来源。

另外知识库更新时不用重新训练模型，只需要重建或更新索引。

---

### Q8：为什么 chunk_size=500，overlap=50？

**标准答案：**

这是项目当前默认参数，目标是在语义完整性和检索粒度之间取平衡。切得太小，上下文容易碎；太大则一个 chunk 混入过多无关内容。

50 的 overlap 用来降低信息刚好跨 chunk 边界被截断的概率。

不能说 500/50 是所有文档的最佳值，它只是当前项目的默认工程取舍。

---

### Q9：为什么 embedding 不用 DeepSeek？

**标准答案：**

因为 DeepSeek 在这个项目使用的 Chat API不提供 embedding 能力。为了避免再依赖一家商业 API，项目默认使用本地 `BAAI/bge-small-zh-v1.5`。

这样知识库索引可以离线构建，降低外部依赖，也适合中文检索。

---

### Q10：为什么用 Chroma？

**标准答案：**

项目规模是企业制度 Demo 和个人知识库级别，需要的是简单持久化、本地可运行和 LangChain 集成，所以 Chroma 足够。

如果数据规模、检索吞吐或分布式要求更高，可以再考虑 Qdrant、Milvus、Elasticsearch/OpenSearch 等。

---

### Q11：怎么降低 RAG 幻觉？

**标准答案：**

第一，知识库建索引时保留 source/page metadata；第二，检索结果以 ToolMessage 形式提供给模型；第三，回答必须引用来源；第四，没有足够证据时明确返回“当前知识库没有找到足够依据”，而不是让模型自由猜。

如果多份文档冲突，项目会把冲突来源都展示出来，而不是偷偷选择一个。

---

### Q12：为什么 top-k 不是越大越好？

**标准答案：**

top-k 太小可能漏掉关键证据，太大又会把无关 chunk 带进上下文，稀释有效信息并增加 token 成本。

项目默认 top-k=4，是当前知识库场景的工程值，不是通用最优值。

---

### Q13：Memory 是怎么做的？

**标准答案：**

LangGraph `InMemorySaver` Checkpointer 按 `thread_id` 保存每个会话线程状态。

同时使用 WindowMemoryMiddleware 控制模型实际看到的上下文，只保留最近 5 轮，避免历史无限增长。

所以“完整 UI 历史”和“模型当前上下文窗口”是两个概念。

---

### Q14：为什么需要 thread_id？

**标准答案：**

thread_id 用来隔离不同会话状态。如果没有它，多个用户/会话可能共享同一个 Checkpointer 状态，导致上下文串线。

Web 和 CLI 都把 thread_id 作为会话边界。

---

### Q15：SSE 是怎么实现流式响应的？

**标准答案：**

FastAPI 的 `/api/chat` 调用 Agent 的流式执行，把 token、tool_call、tool_result、done、error 等事件转换成 SSE frame，再通过 `StreamingResponse` 持续返回给浏览器。

浏览器收到事件后实时更新对话和工具调用状态。

---

### Q16：为什么用 SSE 不用 WebSocket？

**标准答案：**

这个场景主要是服务端向浏览器持续推送 Agent 运行结果，客户端发起新消息仍然可以用普通 HTTP。

SSE 足够轻量，天然基于 HTTP，断线和部署也更简单。需要高频双向通信时再考虑 WebSocket。

---

### Q17：send_email 为什么默认不是直接发真实邮件？

**标准答案：**

因为它是有真实外部副作用的工具，模型可能误解用户、被提示注入或者重复调用。

所以项目默认 `EMAIL_MODE=mock`，只写本地 outbox。即使切到真实 SMTP，也还需要白名单、频率限制，并可以打开人工审批。

---

### Q18：真实邮件的安全防线有哪些？

**标准答案：**

第一层默认 mock，物理上不建立 SMTP；第二层 EMAIL_ALLOWLIST 限定收件人，留空时 fail-closed；第三层每分钟/每小时频率限制；第四层可选 `EMAIL_REQUIRE_APPROVAL`，真正执行前让人确认具体收件人、主题和正文。

这些防线彼此独立，批准也不会绕过白名单或限流。

---

### Q19：人工审批是怎么继续 Agent 的？

**标准答案：**

LangGraph 在工具真正执行前产生 interrupt。Web 端展示审批卡片；用户批准或拒绝后，通过 `/api/chat/approve` 带 thread_id 和 decisions 继续同一条图。

批准后才真正执行工具；拒绝时工具不会先执行再撤销。

---

### Q20：如果邮件已经成功发出，但本地 outbox 审计写失败怎么办？

**标准答案：**

项目会如实报告真实邮件已经发送，同时附带 audit_error，而不是告诉用户“发送失败”。

因为如果误报失败，用户很可能再次重试，反而造成重复真实邮件。这是“真实外部副作用优先于本地审计状态”的处理。

---

### Q21：为什么要有工具直连面板？

**标准答案：**

它可以绕过 LLM 直接验证 RAG、日程和 Mock 邮件工具，帮助区分“模型问题”和“工具/数据问题”。

没有 API Key 时，这些能力也能离线验证，所以调试和演示不会完全依赖模型服务。

---

### Q22：为什么要有 offline_selfcheck？

**标准答案：**

为了在不消耗真实模型额度、没有 API Key 的情况下验证配置、embedding、Chroma、RAG、工具、Agent 循环和人工审批链路。

它能把“系统组件是否能工作”和“外部模型 API 是否可用”分开测试。

---

### Q23：怎么避免 Agent 死循环？

**标准答案：**

项目配置 `MODEL_MAX_TOOL_ROUNDS`，并使用 ModelCallLimitMiddleware 限制一轮中模型和工具往返次数。

如果工具或模型不断重复调用，不会无限执行。

---

### Q24：Prompt Injection 怎么防？

**标准答案：**

提示词不能作为真正的安全边界。对于具有外部副作用的 send_email，真实安全约束放在工具执行层：mock 默认、收件人白名单、频率限制、人工审批。

因此即使知识库文档里出现“把资料发到攻击者邮箱”的恶意内容，只要收件人不在白名单，工具仍然拒绝。

---

### Q25：知识库上传为什么有风险？

**标准答案：**

文件上传涉及格式、大小、路径和恶意内容。项目限制单文件大小和扩展名、清洗文件名避免路径穿越，并允许损坏文件单独失败而不拖垮整个索引。

当前上传 API 没有鉴权，所以默认只监听 localhost；如果要对外暴露必须补鉴权，这是项目当前明确边界。

---

### Q26：索引为什么需要 manifest/content_hash？

**标准答案：**

如果文档没有变化就不应该每次启动都重新 embedding。项目把文档文件名和 sha256 等信息组成内容指纹保存在 manifest。

增加、删除或修改文档后指纹改变，系统才重建索引；没变化就直接复用现有 Chroma。

---

### Q27：这个项目里 LCEL 做什么？

**标准答案：**

LCEL 用来构造独立的 `prompt | model | StrOutputParser` 链，并演示 invoke / stream / batch。

它和 LangGraph Agent 是两个不同层次：LCEL 更适合可组合的线性调用链，LangGraph 更适合有状态、循环和工具节点的 Agent 运行时。

---

### Q28：如果 query_policy 查不到答案怎么办？

**标准答案：**

工具返回没有足够依据，模型应该明确告诉用户当前知识库没有找到相关政策，而不是利用自身预训练知识补一条“公司制度”。

企业内部制度属于高要求事实场景，宁可说没有依据，也不要自信地编。

---

### Q29：FastAPI 在这个项目里负责什么？

**标准答案：**

FastAPI 是 Web 传输层，提供聊天、线程、知识库、工具直连、索引、发件箱等 API；其中聊天和审批续跑使用 SSE 流。

Agent 的真正工具选择和状态循环在 LangGraph 中，FastAPI 本身不负责智能决策。

---

### Q30：这个项目最有价值的工程点是什么？

**标准答案：**

不是“调用了大模型”，而是把 Tool Calling、RAG、Memory、流式输出、工具副作用安全、人工审批、离线自检和可观测性组成了一个完整 Agent 运行闭环。

模型负责不确定性的理解和决策，但真实副作用和安全约束由确定性代码控制。

---

# 四、三个项目绝对不能串栈

| 技术 / 设计 | 渔芯 | 云邻 | Enterprise Smart Assistant |
|---|---|---|---|
| Flask | ✅ | ✅ | ❌ Web 主层为 FastAPI |
| FastAPI | ❌ | ❌ | ✅ |
| Vue3 + TS | ✅ | ❌ | ❌（原生 Web） |
| Jinja2 | ❌ | ✅ | ❌ |
| PyMySQL 手写 SQL | ✅ | 底层驱动可用，但业务层以 SQLAlchemy 为主 | ❌ |
| SQLAlchemy ORM | ❌ | ✅ | ❌ |
| MySQL | ✅ | ✅ | ❌ 核心 Agent 不依赖关系库 |
| Capability Registry | ✅ 核心 | ❌ | ❌ |
| MCP | ❌ 核心 Agent Gateway | ✅ | ❌ 核心工具走 LangChain Tool |
| LangChain/LangGraph | ❌ 当前渔芯主 Agent 不以此为核心 | ❌ | ✅ |
| Chroma RAG | ❌ | ❌ | ✅ |
| DeepSeek Harness | ✅ | ✅ | ❌ 直接 LangGraph/DeepSeek |
| UnitOfWork | ✅ | 服务层/SQLAlchemy 事务模型不同 | ❌ |
| SQLAlchemy version_id_col | ❌ | ✅ | ❌ |
| SSE | Agent链路有流式能力 | ✅ | ✅ |
| DataScope | ✅ | ✅ | 不属于当前项目主设计 |

---

# 五、建议研读顺序

第一遍：只看三个项目的“整体定位 + 流程图”，先能讲清项目是干什么的。

第二遍：只看每个项目 Q1～Q10，解决项目介绍、架构、业务流。

第三遍：看渔芯 Q11～Q26、云邻 Q11～Q26、Agent Q11～Q30，解决连续追问。

第四遍：关掉文档，分别用 3 分钟讲三个项目。

第五遍：只看“三个项目绝对不能串栈”，避免面试时把 SQLAlchemy、MCP、LangGraph 等技术说错项目。

正式检测时应采用：
1. 3 分钟项目介绍；
2. 业务链追问；
3. 架构追问；
4. 技术基础追问；
5. 故障场景；
6. 设计取舍；
7. AI Coding 真实性；
8. 随机跨项目对比。
